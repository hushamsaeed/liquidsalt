---
description: Start work on a task immediately. Plans it, confirms with you, then begins. Shortcut for the plan→confirm→implement flow on clear tasks.
argument-hint: [task description]
---

Task: $ARGUMENTS

Quickly assess: is this task clear enough to plan without clarification?
- If yes (obvious scope, one right approach): plan it in 4–6 bullet points, show the plan inline, ask "Good to go?" — if confirmed, start immediately.
- If ambiguous (multiple valid approaches, architectural decision): use /plan instead to give this more thought.

For "good to go" tasks: once confirmed, implement directly. No separate /plan required.

When done: run /done to verify.
