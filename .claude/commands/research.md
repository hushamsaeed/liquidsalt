---
description: Research a topic using the deep-research skill in a forked context. Returns a summary without polluting main context.
argument-hint: [topic, question, or codebase area]
---

Invoke the `deep-research` skill for: $ARGUMENTS

A forked read-only Explore agent will:
1. Search the codebase for relevant files, types, patterns, and utilities.
2. Read and analyse the most relevant files.
3. Fetch external documentation if needed.
4. Return a concise summary: what exists, patterns in use, recommended approach, specific files to reference.

The summary returns here. Main context stays clean.
